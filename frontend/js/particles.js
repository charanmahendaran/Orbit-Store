/*
 * Orbit Store particle module.
 *
 * Particle systems for Design 1 are currently coordinated
 * directly by scene.js to keep rendering and animation
 * synchronized inside one requestAnimationFrame loop.
 *
 * Reserved for future particle systems that can be safely
 * separated without duplicating render/update work.
 */

export {};
